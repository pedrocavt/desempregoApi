parameters:
    git_dir: .
    bin_dir: vendor/bin
    process_timeout: 280
    tasks:
        phpcsfixer:
            config_file: ~
            config: ~
            fixers: []
            level: ~
            verbose: true